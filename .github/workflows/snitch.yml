name: Snitch Security Scan

on:
  pull_request:
    types: [opened, synchronize, reopened, workflow_dispatch]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  security-events: write
  issues: write
  statuses: write # Snitch sets a check status on the PR head SHA
  models: read # free GitHub Models inference via the auto-issued GITHUB_TOKEN

jobs:
  snitch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Snitch
        uses: snitchplugin/snitch-github-action@v1
        with:
          snitch-license-key: ${{ secrets.SNITCH_LICENSE_KEY }}
          openrouter-api-key: ${{ secrets.OPENROUTER_API_KEY }}
          provider: openrouter
          model: sonnet
      # - name: Post Snitch SARIF findings in the pull request
      #   if: github.event_name == 'pull_request'
      #   uses: sett-and-hive/sarif-to-comment-action@v2.0.1
      #   with:
      #     token: ${{ secrets.GITHUB_TOKEN }}
      #     repository: ${{ github.repository }}
      #     branch: ${{ github.head_ref }}
      #     pr-number: ${{ github.event.number }}
      #     sarif-file: SECURITY_AUDIT_REPORT.sarif
      #     title: Snitch Issue
      #     dry-run: false
